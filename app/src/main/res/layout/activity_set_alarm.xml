<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_set_alarm_from_history"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/back"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ImageButton
        android:id="@+id/Add"
        android:layout_width="352dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:contentDescription="Add"
        android:onClick="imHistoryDeleteAllAlarms_OnClickMethod"
        app:srcCompat="@drawable/ic_add" />

    <ListView
        android:id="@+id/lvAllSetAlarms"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/Add"
        android:layout_marginTop="14dp"
        android:clickable="true"
        android:divider="@drawable/line"
        android:isScrollContainer="true" />

    <TextView
        android:id="@+id/ibDeleteChosenRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignTop="@+id/Add"
        android:layout_marginStart="26dp"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:contentDescription="@string/Edit"
        android:gravity="center"
        android:onClick="imDeleteChosenRow_OnClickListener"
        android:text="@string/Edit"
        android:textSize="25dp" />
</RelativeLayout>
